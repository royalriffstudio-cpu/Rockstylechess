# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.apk
*.aab
android/keystore.properties

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*
expo-dev*.log

# macOS
.DS_Store
*.pem

# editor -- personal Obsidian vault config, not project content
.obsidian/

# local env files -- EXPO_PUBLIC_SERVER_URL is per-developer (LAN IP in dev),
# see .env.example. server/.env.production holds the real Neon connection
# string used only by the `:remote` npm scripts -- see server/README.md.
.env
.env*.local
.env.production

# typescript
*.tsbuildinfo

example

# UI-redesign staging repo (its own git repo, kept checked out here only as a
# fusion reference -- see CLAUDE.md). Not part of this project's tree.
new_ui/

# generated native folders
/ios
/android

# generated from node_modules/stockfish -- run scripts/generate-stockfish-engine.js
src/lib/stockfishEngineAssets.ts
